# Buf code generation configuration for Go

version: v1
managed:
  enabled: true
  go_package_prefix:
    default: charm.land/fantasy/proto/gen
plugins:
  - plugin: buf.build/protocolbuffers/go
    out: gen
    opt:
      - paths=source_relative
